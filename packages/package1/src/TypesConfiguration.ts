export interface TypesConfiguration {
  // valueTypedInFinalProduct: any - the type of valueTypedInFinalProduct const.
  //  Can be any type

  // mapTypedWithFallback: Record<string, number> - the type of mapTypedWithFallback const.
  //  Should be only in the shape of Record<string, number>

  // useNumber: boolean - a flag, which affects the type of valueTypedByFlag const.
  //  Should be only in the shape of boolean - `true` or `false`
}